# asamanew3 — локальное объединение, 16 сентября 2026

Основа: `C:/Users/SamiZnaeteKtoDIR/Downloads/Telegram Desktop/asamanew2`.
Дополнительный источник: соседняя папка `asamanew` с утренними исправлениями.
Обе исходные папки оставлены без изменений. Никаких push, deploy и публикаций.

## Сохранено из asamanew2

- Новые снимки, логотипы, названия «Будь Дома» и «Виктория Черноус».
- Подготовленные изображения со скрытыми контактами.
- Вместо живого iframe — полноэкранная галерея снимков со стрелками,
  клавиатурой, колесом и свайпом. Контакты на снимках не являются ссылками.
- Исключения: `socvetie`, `touch`, `krasivaya` — их материалы не менялись.
- Все остальные страницы, стили и ресурсы новой версии.

## Точечные исправления при объединении

1. `cases/script.js`: картинки орбиты загружаются сразу, `loading=eager`,
   без ожидания свободного времени браузера. Геометрия наведения не заменялась.
2. `cases/masked-materials.js`: параметры версии в URL больше не мешают
   выбрать подготовленное изображение со скрытыми контактами.
3. `index.html` и `for-business/script.js`: оставшееся старое имя заменено
   на «Виктория Черноус».
4. `cases/materials-blurred/belous/orbit-01.png`: у нового снимка телефон
   оказался ниже старой маски. Исправлена только область телефона;
   остальные пиксели и разрешение сохранены. Воспроизводимый сценарий:
   `tools/repair-merged-contact.py`. Обновлены соответствующие координаты
   и размеры в `cases/contact-blur-map.js`.
5. Обновлены версии подключаемых файлов в `cases/index.html`.

Важно: старый обработчик `contact-blur.js` не подключён. Старые инструменты
съёмки и исходные немаскированные материалы сохранены как рабочие файлы;
галерея использует подготовленные PNG. Не запускать старую генерацию масок
поверх новых кадров без проверки координат. Прежние журналы в `tools`
относятся к прежним версиям; актуальные проверки перечислены ниже.

## Проверка

Локальный сервер этой версии: `http://127.0.0.1:4176/`.
Порт `4175` относится к прежней папке, не к asamanew3.

- `node tools/check-belous.mjs --carousel`: два круга карусели и все
  12 орбит на 1440×900, 1024×768 и 390×844.
- `node tools/check-belous.mjs --merged`: все кадры полноэкранной галереи,
  навигация, новые имена, отсутствие iframe и ссылок внутри просмотра;
  ошибки JavaScript и сетевой загрузки.
- `node tools/check-belous.mjs`: наведение на семь внешних кадров Черноус,
  стабильность выбора, совпадение чёткого слоя, стрелки и мобильный свайп.

Механизм `renderFrames`, определение кадра под курсором `frameAtPoint`
и файл `sharp-preview.js` совпадают с утренней версией. Ночные дополнения
CSS касаются полноэкранного просмотра, а не анимации орбиты.

Итог проверки: все три команды завершились успешно. Проверены 12 проектов
и все 125 кадров, фактически перечисленных в этой версии, на трёх размерах
экрана. Отчёты: `tools/.quality-check/static-loading-checks.json` и
`tools/.quality-check/merged-gallery-checks.json`. Полноэкранная проверка
не выявила ошибок JavaScript или загрузки. Наведение на орбите Черноус
стабильно на всех семи внешних кадрах, чёткий слой совпадает с изображением;
клавиатура, стрелки и мобильный свайп работают. Мигание не воспроизвелось.

Из 257 изображений и ресурсов в `cases/materials`, `cases/materials-blurred`
и `cases/assets` ровно один отличается от asamanew2: исправленная область
телефона в `materials-blurred/belous/orbit-01.png`. Остальные совпадают побайтно.
